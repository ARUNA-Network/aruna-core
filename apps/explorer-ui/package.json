{
  "name": "aruna-explorer-ui",
  "version": "1.0.0",
  "description": "ARUNA Network Block Explorer UI",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp *.html style.css app.js dist/"
  }
}
